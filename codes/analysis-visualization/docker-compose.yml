version: '2'
services:
  python_study:
    build:
      context: ./analyze_pycode
      dockerfile: dockerfile
    container_name: python_study
    volumes:
    - ./analyze_pycode:/
    command: /bin/sh -c "python /torch_lstm.py"
